# VoiceGuide FastAPI server runtime dependencies.
# The deployed server receives on-device detection JSON from Android; it does
# not run YOLO, Depth, ONNX export, or dataset collection.

fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.12
python-dotenv>=1.0.0
requests>=2.31.0

# Optional production database support. Without DATABASE_URL, the server uses
# local SQLite.
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
