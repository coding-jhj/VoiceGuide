# 2026-05-08 변경 이력

> 범례: ✅ 반영 완료 | ❌ 반영 실패/롤백 | ⚠️ 부분반영/미검증 | 🗑️ 제거

---

## 핵심 이벤트

오늘은 **버그 집중 수정 + 문서화** 가 이루어진 날.

1. **detect_json 저장 버그 수정** (feature/jaehyun) — recent_detections DB 미저장 문제 해결
2. **심각 버그 4건 수정** — session_id 불일치, DB 성능 버그, EMA 평활화 미작동, 테스트 불안정
3. **경고 및 개선 이슈 11건 수정** — Android·서버·policy.json 전반 정리
4. **feature/myunggwang 머지** — 대시보드 수정 및 데모 시뮬레이션 추가
5. **프로젝트 전체 문서화** — ARCHITECTURE_DIAGRAM, CURRENT_STATUS_REPORT, SIMULATION_RESULTS, system_analysis 신규 작성

---

## 수정 전 / 수정 후 — detect_json 저장 버그 (feature/jaehyun)

| 항목 | 수정 전 | 수정 후 | 상태 |
|---|---|---|---|
| /detect_json DB 저장 | recent_detections 미저장 | 정상 저장 | ✅ |
| MainActivity.kt | gradlew 실행 권한 누락 | 권한 부여 완료 | ✅ |

---

## 수정 전 / 수정 후 — 심각 버그 4건

| 항목 | 수정 전 | 수정 후 | 상태 |
|---|---|---|---|
| routes.py save_snapshot | wifi_ssid 기준으로 스냅샷 저장 → 기기 간 오염 | session_id로 통일 | ✅ |
| db.py PostgreSQL save_detections | N건 탐지 시 N번 DELETE 실행 | executemany + 루프 밖 단일 DELETE | ✅ |
| tracker.py monkey-patch | _mvp_update/_mvp_get_current_state 외부 주입 → _tracks 키 불일치로 EMA 미작동 가능 | 클래스 메서드로 이동, monkey-patch 제거 | ✅ |
| test_api.py 세션 검증 | device_id 직접 사용 → wifi_ssid 변경 시 조용히 실패 | _normalize_session_id로 session_id 명시 | ✅ |

---

## 수정 전 / 수정 후 — 경고 및 개선 이슈 11건

### 경고

| 항목 | 수정 전 | 수정 후 | 상태 |
|---|---|---|---|
| routes.py /status·/events·/history | session_id를 그대로 전달 | _normalize_session_id(device_id=session_id)로 수정 | ✅ |
| events.py publish() | def (동기) | async def + await 호출로 변환 | ✅ |
| policy.json on_device | bbox_calib_area_by_class 누락 → 서버 policy 수신 시 거리 보정 비활성화 | 섹션 추가 | ✅ |
| VoiceGuideConstants.kt CLOCK_TO_DIRECTION | "12시" 방향 문자열, 5·6·7시 없음 | "바로 앞"으로 수정, 5·6·7시 방향 추가 | ✅ |
| TfliteYoloDetector.kt | .first {} → NPE 가능 | .firstOrNull {} + 명시적 예외 처리 | ✅ |
| MvpPipeline.kt update() | 동기화 없음 | @Synchronized 추가 | ✅ |

### 개선

| 항목 | 수정 전 | 수정 후 | 상태 |
|---|---|---|---|
| db.py cutoff 계산 | datetime.now() (로컬 시간) | datetime.now(timezone.utc) 적용 | ✅ |
| test_server.py /locations | 미구현 엔드포인트 테스트 실패 | `/locations` 라우트 구현 후 skip 제거 | ✅ |
| requirements.txt | torch/torchvision/ultralytics 포함 → Docker 이미지 비대 | dev-only 주석 처리 | ✅ |
| simulator.py 헤더 | X-Api-Key | X-API-Key로 통일 | ✅ |

---

## 수정 전 / 수정 후 — feature/myunggwang 대시보드 및 시뮬레이션

| 항목 | 수정 전 | 수정 후 | 상태 |
|---|---|---|---|
| 대시보드 UI | 이전 버전 | 수정 반영 | ✅ |
| tools/simulator.py | 미존재 | 데모용 GPS 동선 시뮬레이터 추가 | ✅ |

---

## 신규 문서

| 파일 | 내용 |
|---|---|
| docs/ARCHITECTURE_DIAGRAM.md | 전체 아키텍처 다이어그램 |
| docs/CURRENT_STATUS_REPORT.md | 현재 프로젝트 상태 보고서 |
| docs/SIMULATION_RESULTS.md | 시뮬레이션 결과 정리 |
| docs/system_analysis.md | 시스템 전체 분석 (기존 두 파일 통합) |
| docs/debug_analysis.md | 버그 원인 분석 보고서 |
| docs/debug_report.md | 디버그 리포트 |
| tests/test_simulation.py | 시뮬레이션 테스트 코드 |

---

## 주요 커밋

| 커밋 해시 | 메시지 |
|---|---|
| `d93dbb4` | Fix detect_json storage and MVP docs |
| `b5b9c04` | fix: 심각 버그 4건 수정 |
| `806f98b` | fix: 경고 및 개선 이슈 11건 수정 |
| `3bae5e1` | feature/myunggwang: 대시보드 수정 및 데모시뮬레이션 |
| `c202054` | docs: 시스템 전체 분석 문서 추가 |
| `3a0697a` | docs: 버그 원인 분석 보고서 추가 |
| `8fda82c` | docs: system_analysis.md 두 파일 합치고 실제 코드 기준으로 수정 |
| `3cafcee` | Update project docs and simulation test |

---

## 내일 우선순위

1. **GCP 재배포** — 오늘 수정 사항 반영 확인 (tracker EMA, session_id 정규화)
2. **실기기 테스트** — yolo11n_320 vs yolo26n FPS·정확도 비교
3. **대시보드 검증** — 시뮬레이터로 GPS 동선 재생 후 실시간 표시 확인
4. **confThreshold 조정** — 0.40 기준 오탐·미탐 균형 실험
