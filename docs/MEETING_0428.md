# 4/28 미팅 준비 (10:00)

---

## 한 줄 요약

> 4/27 강사님 피드백 3가지 전부 반영 완료 + 기능 고도화 지속 중.

---

## 1. 어제 강사님 피드백 → 반영 결과

| 피드백 | 반영 여부 | 구현 내용 |
|-------|---------|---------|
| 경고가 너무 많다 — 음성/비프음 분리 | ✅ 완료 | `alert_mode` 3단계 (critical/beep/silent) 구현, Android 앱 연동 |
| 거리 수치 말하지 마라 | ✅ 완료 | "약 1.2m" → "가까이 / 매우 가까이 / 조금 멀리" 상대 표현으로 전환 |
| 타겟 유저를 전맹으로 명확히 해라 | ✅ 반영 | 발표 자료에 "전맹(완전 시각 장애)을 주 타겟으로 설계" 명시 |

---

## 2. 어제 추가로 수정한 것

### 버그 수정

| 항목 | 내용 |
|------|------|
| TTS 목소리 겹침 완전 해결 | STT 시작 시 TTS 즉시 중단, stale 재생 방지, MediaPlayer 단일 인스턴스 |
| STT "네" 무반응 해결 | TTS 완료 후 자동으로 STT 시작하도록 `UtteranceProgressListener` 적용 |
| 탐지 텍스트 너무 빨리 사라지는 문제 | 탐지 결과 3초 유지 후 "장애물 없음" 전환 |
| `stairs` 오탐 방지 | 최소 신뢰도 0.50 → 0.72 (키보드·바닥 패턴이 계단으로 오탐되던 문제) |

### TTS 고도화

- **Naver Clova Voice** 연결 완료 (nara 목소리) — `.env`에 키만 넣으면 자동 전환
- ElevenLabs SDK 완전 제거 → REST API 직접 호출 (websockets 충돌 해결)

### 팀원 브랜치 반영

- **임명광 (feature/nlg)**: 경고 피로 방지 함수 / 문구 자연스럽게 수정 / 바운딩박스 오버레이
- **문수찬 (feature/tts)**: ElevenLabs TTS 교체
- **신유득 (feature/api)**: Supabase/PostgreSQL 서버 (`서버_DB/`)

---

## 3. 현재 구현 완료된 기능 전체

### 핵심 파이프라인

- YOLO11m 장애물 탐지 (COCO 80클래스 + 계단 파인튜닝, mAP50 0.992)
- Depth Anything V2 상대 거리 추정 (GPU 실시간)
- 깊이 맵 기반 계단·낙차·턱 감지
- 경고 계층 분리 — critical(즉각 음성) / beep(비프음) / silent(무음)
- 한국어 조사 자동화 + 긴박도별 문장 생성

### Android 앱

- 1초마다 자동 캡처 → 온디바이스 ONNX 추론 (서버 없이 동작)
- STT 음성 명령 22가지 모드
- 카메라 방향 자동 감지 (가속도 센서)
- 개인 네비게이팅 ⭐ (장소 저장/찾기)
- 보호자 SOS ⭐, 낙상 감지 ⭐, 버스 번호 인식 ⭐
- 지폐 인식, 식사 도우미 모드, GPS 하차 알림, 약 복용 알림

---

## 4. 데모 시나리오 (실기기)

> 기기: Samsung Galaxy Z Fold 4 / 서버: RTX 5060 PC

1. 앱 시작 → "음성 안내를 시작할까요?" 자동 음성 → "네"로 시작
2. 장애물 앞에서 **1초마다 자동 분석** → "바로 앞에 의자가 있어요. 기다리세요."
3. "가방 찾아줘" → "가방은 왼쪽 앞 가까이 거리에 있어요."
4. "여기 저장해줘 편의점" → "편의점을 저장했어요."
5. 차량 접근 → `critical` 모드로 즉각 음성 끊고 경고

---

## 5. 발표 때 강조할 포인트

1. **경고 피로 해결** — critical/beep/silent 3단계로 불필요한 음성 최소화
2. **거리 표현 솔직함** — 단안 카메라 한계 인정, 수치 대신 상대 표현 사용
3. **전맹 타겟 설계** — 버튼 없이 음성만으로 전체 조작 가능
4. **서버 없이도 동작** — 온디바이스 ONNX로 오프라인 동작

---

## 6. 구현 완료 (추가 확인 사항)

아래 항목들은 **모두 이미 구현되어 있음** — 미팅에서 데모 가능.

| 항목 | 상태 | 구현 위치 |
|------|------|---------|
| 신호등 빨강/초록 감지 | ✅ 완료 | `src/vision/detect.py` — YOLO bbox HSV 분석, `traffic_light_msg` 반환 |
| 울퉁불퉁한 길 감지 | ✅ 완료 | `src/depth/hazard.py` — Depth 맵 바닥 분산 분석 |
| 점자 블록 위 장애물 | ✅ 완료 | `src/vision/detect.py` — 이미지 하단 중앙 경로 위 장애물 감지 |
| 방향 좌우 반전 버그 | ✅ 완료 | `src/vision/detect.py:362` — `cv2.flip(img, 1)` 적용 |
| 버스 번호 OCR | ✅ 완료 | `src/ocr/bus_ocr.py` + `/ocr/bus` 엔드포인트 |

> 키오스크 접근성만 미구현 (어제 강사님이 새로 제안한 아이디어)

---

## 7. 개발 중 마주친 문제 & 해결 과정

### 라이브러리 호환성

| 문제 | 원인 | 해결 |
|------|------|------|
| `numpy 'bool' AttributeError` | NumPy 2.x가 OpenCV·Ultralytics와 호환 불가 | `numpy==1.26.4` 고정 |
| `gradio ImportError starlette` | Gradio 5.x가 FastAPI starlette 버전 충돌 | `gradio==4.44.1` 고정 |
| `websockets.asyncio` 없음 | ElevenLabs SDK가 websockets 13+ 요구, gradio-client는 <13 요구 | ElevenLabs SDK 제거 → REST API 직접 호출로 교체 |
| TFLite 변환 실패 | Python 3.13은 tensorflow 2.19 이하 미지원 | ONNX 포맷으로 대체 (ONNX Runtime Android 사용) |

### 외부 서비스

| 문제 | 원인 | 해결 |
|------|------|------|
| ElevenLabs 402 오류 | 무료 플랜에서 보이스 라이브러리 정책 변경 | gTTS(기본) + Naver Clova(키 있을 때) 구조로 교체 |

### Android 앱 버그

| 문제 | 원인 | 해결 |
|------|------|------|
| TTS 목소리 겹침 | 분석 응답마다 MediaPlayer 새로 생성, stale 재생 | 단일 인스턴스 관리 + `ttsRequestId`로 stale 차단 |
| STT "네" 말해도 무반응 | TTS 말하는 중에 마이크 열려서 TTS 목소리를 STT가 인식 | `UtteranceProgressListener` — TTS 끝난 후 600ms 대기 → STT 시작 |
| 계단 오탐 (키보드·에스컬레이터) | 계단형 패턴을 낮은 신뢰도로 통과 | `stairs` 최소 신뢰도 0.50 → 0.72 |

---

## 8. 현재 알려진 한계

| 항목 | 내용 |
|------|------|
| 거리 정밀도 | 단안 카메라 + Depth V2는 상대 추정 — "가까이/멀리" 표현 사용, 수치 제거 |
| 투명 장애물 | 유리문·유리벽 탐지 불가 (AI 공통 한계) |
| 연속 스트리밍 | 1초 간격 캡처 방식 (실시간 영상 처리 아님) |
| 비정형 계단 | 나선형·야외 계단은 실환경 추가 테스트 필요 |

---

*2026-04-28 미팅 준비*
