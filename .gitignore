# API 키 / 환경 변수
.env

# AI 모델 가중치 (용량 큼 — 별도 공유)
*.pth
*.pt
*.onnx
*.onnx.data
*.tflite

# Python 캐시
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store

# 테스트 이미지 / 데이터셋 (용량 큼 — 별도 공유)
data/test_images/
datasets/

# 학습 실행 결과 (로컬 전용)
runs/

# TTS 캐시 (자동 재생성)
__tts_cache__/

# SQLite DB (로컬 데이터)
voiceguide.db

# Android 빌드 산출물
android/app/build/
android/.gradle/
android/local.properties
