# Claude Code 로컬 설정 (개인 설정 — 커밋 불필요)
.claude/

# 생성된 산출물 / 임시 파일
outputs/
tmp/

# API 키 / 환경 변수 — 실제 키가 담길 수 있는 파일은 모두 제외
.env
.env.*
.env.example
*.env

# AI 모델 가중치 (용량 큼 — 별도 공유)
models/
*.pth
*.pt
*.onnx
*.onnx.data
*.tflite
!android/app/src/main/assets/yolo26n_float32.tflite
!android/app/src/main/assets/yolo11n_320.tflite

# Python 캐시
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ultralytics/
.cursor/
.idea/
.vscode/
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store

# 테스트 이미지 파일 (용량 큼 — 별도 공유). 폴더 구조는 .gitkeep으로 유지
data/test_images/**/*.jpg
data/test_images/**/*.jpeg
data/test_images/**/*.png
data/test_images/**/*.JPG
data/test_images/**/*.JPEG
data/test_images/**/*.PNG
datasets/

# 학습 실행 결과 (로컬 전용)
runs/
flagged/

# TTS 캐시 (자동 재생성)
__tts_cache__/

# SQLite DB (로컬 데이터)
voiceguide.db

# 원시 성능 로그 (분석 후 결과만 보존)
perf_log.txt

# Android 빌드 산출물
android/app/build/
android/.gradle/
.gradle-*/
android/local.properties
서버_DB_수정/__pycache__/
서버_DB_수정/%s
서버_DB_수정/ConnectionPool
서버_DB_수정/dict[str
서버_DB_수정/RoadGlass

# remotion-video 렌더링 산출물 (용량 큼 — 별도 공유)
remotion-video/out/
remotion-video/debug-frames/
remotion-video/source-check/
remotion-video/node_modules/
remotion-video/public/assets/demo.mp4

# vision_ML/ (Phase 4 Vision·ML 작업 트리 — 중첩 빌드·로컬 설정)
vision_ML/android/app/build/
vision_ML/android/.gradle/
vision_ML/android/local.properties
vision_ML/data/test_images/**/*.jpg
vision_ML/data/test_images/**/*.jpeg
vision_ML/data/test_images/**/*.png
vision_ML/data/test_images/**/*.JPG
vision_ML/data/test_images/**/*.JPEG
vision_ML/data/test_images/**/*.PNG
